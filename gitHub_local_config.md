# config the local folder github
- `cd /path/to/specific/repo`
- `git config user.name "Reggie Lu"`
- `git config user.email "reginold.lu@gmail.com"`
# Verify
- `git config user.name`
- `git config user.email`

